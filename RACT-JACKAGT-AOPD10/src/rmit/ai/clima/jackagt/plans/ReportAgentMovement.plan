package rmit.ai.clima.jackagt.plans;
import rmit.ai.clima.jackagt.events.*;
import rmit.ai.clima.jackagt.data.*;
import rmit.ai.clima.interfaces.*;
import rmit.ai.clima.gui.grid.GridPoint;


public plan ReportAgentMovement extends Plan
{
   /******** Start PDT Design Block *** DO NOT EDIT IT *********/

/*
Plan Name: ReportAgentMovement
*/
	//Events handled by the plan are declared here.
	#sends event MEInformAgentStatus meinformagentstatus_s;

	#handles event EAgentMoved eagentmoved_h;


	// Declarations of any beliefset/data that the plan accesses.
/******** End PDT Design Block *** DO NOT EDIT IT *********/
   // The enclosing agent or capability has to implement these interfaces
   #uses interface DebugInterface consoleIface;

   context()
   {
      // Trigger: Event "EAgentMoved"
      true;
   }

   body()
   {

      int posX = eagentmoved_h.xCoord;
      int posY = eagentmoved_h.yCoord;

      @send("boss", meinformagentstatus_s.post(getAgent().name(),new GridPoint(posX,posY)));


   }
}
